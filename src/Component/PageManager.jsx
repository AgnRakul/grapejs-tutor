import React from "react";
import { Button } from "react-bootstrap";

const PageManager = () => {
  return (
    <div id="page-manager" className="page-manager">
      <div className="page-container">
        <Button>Add Page</Button>
      </div>
    </div>
  );
};

export default PageManager;
